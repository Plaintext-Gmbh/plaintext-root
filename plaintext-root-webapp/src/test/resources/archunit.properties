# Rules whose "that" filter legitimately matches nothing must not fail:
# fwtool has no periodic runs, schuetu has no PlaintextCron implementations, and so on.
# The actual prohibition still bites as soon as an @Scheduled/Cron is added.
# IMPORTANT: the key is camelCase (archRule), not archrule.
archRule.failOnEmptyShould=false
