{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "HIER STAND 'ignorePaths': ['Dockerfile'] (Karte 1302, 22.09.2026). Der Eintrag ist am",
    "23.09.2026 mit Karte 1325 zusammen mit ./Dockerfile entfernt worden (Entscheid worker nach",
    "Messung) — bewusst mit dieser Notiz statt ersatzlos, damit die Begruendung auffindbar bleibt.",
    "WARUM: ./Dockerfile wurde seit dem Initial-Commit (30.07.2026) nie geaendert und von niemandem",
    "gebaut. plaintext-root traegt den Marker .m3-jar-volume (Jar im Volume statt docker build);",
    "weder .woodpecker/ noch .github/ noch plaintext-scripts noch ein Compose-File referenziert ihn.",
    "Positivkontrolle derselben Suche: sie findet den echten `docker build` in",
    "plaintext-scripts/runtime/build-runtime.sh. Wie am 08.09.2026 in app/iot/schuetu.",
    "NEBENWIRKUNG, die kein Verlust ist: 'ignorePaths' ERSETZTE Renovates Voreinstellung",
    "(node_modules, bower_components, Test-Fixtures, ...); mit dem Wegfall gilt sie wieder.",
    "Gepflegt wird das Basis-Image ALLER App-Container dort, wo es wirklich gebaut wird:",
    "plaintext-scripts/runtime/Dockerfile (eigene renovate.json, Karte 1138)."
  ],
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Security-Pin tomcat.version: ueberschreibt die vom Spring-Boot-Parent vorgegebene Tomcat-Version. Steht ohne eigene <dependency>-Deklaration in den Properties und waere fuer Renovate sonst unsichtbar (Karte 389).",
      "managerFilePatterns": [
        "/^pom\\.xml$/"
      ],
      "matchStrings": [
        "<tomcat\\.version>(?<currentValue>[^<]+)</tomcat\\.version>"
      ],
      "depNameTemplate": "org.apache.tomcat.embed:tomcat-embed-core",
      "datasourceTemplate": "maven"
    },
    {
      "customType": "regex",
      "description": "Security-Pin httpcore5.version (siehe tomcat.version).",
      "managerFilePatterns": [
        "/^pom\\.xml$/"
      ],
      "matchStrings": [
        "<httpcore5\\.version>(?<currentValue>[^<]+)</httpcore5\\.version>"
      ],
      "depNameTemplate": "org.apache.httpcomponents.core5:httpcore5",
      "datasourceTemplate": "maven"
    },
    {
      "customType": "regex",
      "description": "Security-Pin jackson-bom.version (siehe tomcat.version).",
      "managerFilePatterns": [
        "/^pom\\.xml$/"
      ],
      "matchStrings": [
        "<jackson-bom\\.version>(?<currentValue>[^<]+)</jackson-bom\\.version>"
      ],
      "depNameTemplate": "tools.jackson:jackson-bom",
      "datasourceTemplate": "maven"
    },
    {
      "customType": "regex",
      "description": "Security-Pin swagger-ui.version: kommt transitiv aus springdoc, das die Version nicht selbst verwaltet (Karte 389).",
      "managerFilePatterns": [
        "/^pom\\.xml$/"
      ],
      "matchStrings": [
        "<swagger-ui\\.version>(?<currentValue>[^<]+)</swagger-ui\\.version>"
      ],
      "depNameTemplate": "org.webjars:swagger-ui",
      "datasourceTemplate": "maven"
    }
  ]
}
